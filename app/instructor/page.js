'use client';
import {useState} from 'react';
export default function Instructor(){const [sites,setSites]=useState(['https://www.myopenmath.com','https://www.statkey.org']);const [site,setSite]=useState('');return <main><h1>Instructor Exam Setup</h1><section><label>Exam name<input defaultValue="Statistics Exam 2"/></label><label>Duration (minutes)<input type="number" defaultValue="90"/></label></section><section><h2>Allowed applications</h2><label className="check"><input type="checkbox" defaultChecked/> Microsoft Excel</label><label className="check"><input type="checkbox"/> Calculator application</label></section><section><h2>Allowed websites</h2>{sites.map((s,i)=><div className="pill" key={i}>{s}<button onClick={()=>setSites(sites.filter((_,j)=>j!==i))}>Remove</button></div>)}<div className="row"><input placeholder="https://approved-site.edu" value={site} onChange={e=>setSite(e.target.value)}/><button onClick={()=>{if(site){setSites([...sites,site]);setSite('')}}}>Add website</button></div></section><section><h2>Proctoring settings</h2>{['Require webcam','Require microphone','Require entire-screen share','Record session','Log focus changes','Flag screen-share interruption'].map((x,i)=><label className="check" key={x}><input type="checkbox" defaultChecked={i!==1}/>{x}</label>)}</section><section><h2>LMS integration</h2><p>This MVP includes the instructor/student workflow. Production Canvas and D2L integration should be added as an LTI 1.3 service after institutional registration, keys, redirect URLs, and deployment IDs are available.</p><label>LMS<select><option>Canvas</option><option>D2L Brightspace</option></select></label></section><button className="primary" onClick={()=>alert('Prototype settings saved in this demo only.')}>Save Exam Settings</button></main>}